/**
 * World-map checkpoint plate. The chip face is composited in the UI so
 * state tints can sit on top without baking shadows into the bitmap.
 */
export const mapNodeArt = {
  plate: require('../assets/brand/chips/map-node-plate.png'),
} as const;
